# План этапа 1: расходы и доходы через бота

- **Этап:** 1, `docs/architecture.md`, раздел 13
- **Ветка:** `feat/stage1-bot-ledger`
- **Заведён:** 2026-09-23

Файл плана этапа — не задание исполнителю. Он лежит в `docs/tasks/`, пока этап
открыт, и переносится в `docs/tasks/done/`, когда закрыта последняя подзадача.
На каждую подзадачу главная сессия заводит отдельный файл задачи по шаблону
`docs/tasks/README.md` и отмечает здесь чекбокс после приёмки, записи в журнал
и коммита. Одна сессия-диспетчер закрывает одну подзадачу; подзадачи, помеченные
как параллельные, запускаются одним сообщением в одной сессии.

Цель этапа: пользователь ведёт учёт расходов и доходов целиком в Telegram.
Работают `/start` с онбордингом и дефолтным деревом категорий, диалоги
`/expense` и `/income`, быстрый ввод одной строкой, `/balance`, `/today`,
`/month` и `/undo` (сторно). `User`, `Account`, `Category`, `Transaction`
хранятся в PostgreSQL. Одна валюта — базовая валюта пользователя, курсов нет.

Общие границы этапа, которые повторяются в каждом файле задачи: нет курсов
валют, Redis, HTTP-эндпоинтов для SPA и `frontend/`; FSM — `MemoryStorage`;
проведённые операции не изменяются и не удаляются, отмена — только сторно
(раздел 4.2); каталоги вне дерева раздела 2.1 не создаются.

Кто пишет тесты. `developer` пишет продуктовый код и держит зелёными уже
существующие `make test` и `make lint`, включая `tests/unit/test_layering.py`.
Новые тесты слоя пишет `tester` отдельной подзадачей после кода слоя.

## Решения, принятые при составлении плана

- **`make seed` и `make recalc` в этап 1 не входят.** По разделу 10.6 `seed`
  создаёт демо-данные вместе со вкладом, а вклады появляются на этапе 6; урезанный
  `seed` из пользователя и категорий дублировал бы онбординг, который и так
  проверяется сквозной подзадачей через `/start`. `recalc` пересчитывает кэши из
  журнала, а в этапе 1 кэшей нет: остаток считается из журнала на лету, первый
  кэш — `balance_snapshots` на этапе 3. Заглушки в `Makefile` остаются
  заглушками и ссылаются на этапы, где у команд появляется содержимое: `seed` —
  этап 2 (демо-данные нужны SPA), `recalc` — этап 3.
- **`ThrottlingMiddleware` из раздела 6.2 в этап 1 не входит**: счётчик живёт в
  Redis, а Redis появляется на этапе 2. Остальные middleware — `Logging`,
  `User`, `Error` — входят. `UnitOfWorkMiddleware` из документа убран при
  заведении подзадачи 4: транзакцию открывает use case (раздел 2.2).
- **Пробелы документа, которые диспетчер закрывает правкой
  `docs/architecture.md` при заведении соответствующей подзадачи**, а не
  перекладывает на исполнителя:
  - состав дефолтного дерева категорий в документе не описан — фиксируется в
    разделе 3.3 до подзадачи 1;
  - каталога `tests/unit/application/` для тестов use case в дереве 2.1 нет, хотя
    уровень «модульные с заглушками» в 11.1 есть — добавляется в 2.1 до
    подзадачи 6;
  - в разделе 6.2 нет роутера для `/undo`, а `common.py` и `fallback.py` из 6.2
    отсутствуют в дереве 2.1; в разделе 6.3 нет состояний онбординга, хотя 6.1
    требует выбрать базовую валюту, таймзону и первый счёт — всё это решается в
    2.1, 6.2 и 6.3 до подзадачи 12b.
  - в таблице `categories` раздела 4.3 нет столбца `aliases text[]`, на
    который опирается разрешение категории в 6.5, — решается в 3.3 и 4.3 до
    подзадачи 8.

## Подзадачи

### domain

- [x] **1. Доменное ядро: деньги, перечисления, сущности.** Исполнитель
  `developer`. Разделы 2.2, 3.1–3.4, 4.2. `domain/common/` — `money.py`,
  `currency.py`, `period.py`, `errors.py`; `domain/entities/` — `user.py`,
  `account.py`, `category.py`, `transaction.py` с правилом сторно;
  перечисления из 3.2; дефолтное дерево категорий. Приёмка: файлы существуют;
  `grep -rn "float" src/finplan/domain` пуст; `make test` и `make lint`
  зелёные; `git diff --stat` не выходит за `src/finplan/domain/`.
- [x] **2. Модульные тесты домена.** Исполнитель `tester`. Разделы 11.1, 3.1,
  4.2. Тесты в `tests/unit/domain/`: арифметика и округление `Money`, запрет
  смешения валют, полуинтервал `Period`, инварианты знака и типа
  `Transaction`, сторно, целостность дефолтного дерева. Приёмка: `make test`
  зелёный, новые тесты только в `tests/unit/domain/`, `src/` не изменён.
- [x] **3. Ревью домена.** Исполнитель `reviewer`. Разделы 2.2, 3.1–3.4.
  Предмет — `git diff` подзадач 1–2. Приёмка: список находок с `file:line` в
  файле задачи; находки уровня «нарушение» исправлены тем же `developer`
  или `tester` до отметки чекбокса.

### application

- [x] **4. Порты и DTO.** Исполнитель `developer`. Разделы 2.2, 3.3.
  `application/ports/repositories.py`, `uow.py`, `clock.py`; DTO входа и
  выхода всех use case этапа в `application/dto/`. Приёмка: протоколы и DTO
  существуют, импортируют только `domain` и стандартную библиотеку с pydantic;
  `make test` и `make lint` зелёные; `src/finplan/domain/` не изменён.
- [x] **5a. Use case записи.** Исполнитель `developer`. Разделы 2.2, 4.2,
  6.1. Онбординг с созданием счёта и дефолтного дерева категорий и поиск
  пользователя по `telegram_id` для `UserMiddleware` (`use_cases/auth/`), запись расхода и дохода, сторно последней операции,
  введённой в боте (`use_cases/transactions/`). Каждый — класс с `__call__`,
  DTO на входе и выходе, транзакция через UoW. Приёмка: `make test` и
  `make lint` зелёные; `domain/` и `ports/` не изменены.
- [x] **5b. Use case чтения.** Исполнитель `developer`. Разделы 2.2, 3.1,
  6.1. Остатки по счетам с итогом в базовой валюте (`use_cases/accounts/`),
  сводки за день и за текущий месяц в таймзоне пользователя по полуинтервалу
  (`use_cases/reports/`), списки счетов и категорий для клавиатур диалогов
  (`use_cases/accounts/`, `use_cases/categories/`). Приёмка та же, что у 5a. Параллельна 5a: общих
  файлов нет, результатов друг друга не читают.
- [x] **6. Тесты use case.** Исполнитель `tester`. Разделы 11.1, 4.2.
  In-memory реализации портов и тесты всех use case этапа в
  `tests/unit/application/`. Приёмка: `make test` зелёный; `src/` не изменён;
  есть тест, что сторно не меняет исходную операцию, а создаёт новую.
- [x] **7. Ревью слоя application.** Исполнитель `reviewer`. Разделы 2.2,
  4.2. Предмет — `git diff` подзадач 4–6. Приёмка как у подзадачи 3.
  По объёму разбита на две параллельные задачи ревью: 7a — код и правки
  документа (`2026-09-24-application-code-review.md`), 7b — тесты
  (`2026-09-24-application-tests-review.md`).
  Нарушения закрыты подзадачами 7c — код
  (`2026-09-24-application-review-fixes.md`) и 7d — тесты
  (`2026-09-24-application-review-tests.md`).

### infrastructure

- [x] **8. ORM-модели и миграция.** Исполнитель `developer`. Разделы 4.1,
  4.3, 4.5. Модели `accounts`, `categories`, `transactions` в
  `infrastructure/db/models/`, миграция Alembic с политиками RLS. Приёмка:
  `make up` и `make migrate` проходят; `uv run alembic downgrade -1` и
  повторный `upgrade head` проходят; `tests/integration/test_migrations.py`
  зелёный; `make lint` зелёный. После решений по RLS для `users` и ролям
  базы (раздел 4.5) разбита по объёму на две последовательные подзадачи:
  8a — таблицы (`docs/tasks/done/2026-09-24-ledger-orm-models.md`), 8b — роли,
  RLS и функция поиска пользователя
  (`docs/tasks/done/2026-09-24-rls-and-db-roles.md`).
- [x] **9a. Репозитории, Unit of Work, RLS.** Исполнитель `developer`.
  Разделы 4.1, 4.5, 2.2. `infrastructure/db/repositories/`, `db/uow.py`,
  `db/rls.py`. Приёмка: реализации удовлетворяют протоколам из
  `application/ports/` по `mypy`; `make test` и `make lint` зелёные;
  `application/` и `domain/` не изменены.
- [x] **9b. Запросы остатков и сводок.** Исполнитель `developer`. Разделы
  4.3, 3.1. `infrastructure/db/queries/`: остаток по счёту из журнала, сводки
  за период `[from, to)`. Приёмка как у 9a. Параллельна 9a: 9b опирается на
  модели из подзадачи 8 и порты из 4, а не на репозитории.
- [x] **9c. `occurred_on` в таймзоне пользователя.** Исполнитель
  `developer`, слои domain и application, затем infrastructure. Разделы
  3.3, 4.3. Раздел 4.3 требует хранить в `transactions.occurred_on` дату
  операции в таймзоне пользователя, а репозиторий из 9a пишет
  календарную дату `occurred_at` в UTC: доменная `Transaction` таймзоны
  не знает, а `RecordTransaction` её не передаёт. Код расходится с
  документом, и расхождение отмечено комментарием в
  `infrastructure/db/repositories/transactions.py`. Журнал неизменяем,
  поэтому неверную дату нельзя исправить потом: подзадача закрывается до
  entrypoints, то есть до первой записи операций ботом. Выбрано поле
  `occurred_on` в доменной `Transaction`, которое `Transaction.new`
  вычисляет из переданного `tzinfo`; передача таймзоны в репозиторий
  отвергнута, обоснование в 3.3. Разбита по слоям: domain
  (`2026-09-24-transaction-occurred-on-domain.md`), затем параллельно
  application (`2026-09-24-transaction-occurred-on-application.md`) и
  infrastructure (`2026-09-24-transaction-occurred-on-infrastructure.md`).
- [x] **10. Интеграционные тесты инфраструктуры.** Исполнитель `tester`.
  Разделы 11.1, 11.4, 4.5. Репозитории, запросы и изоляция пользователей
  против PostgreSQL в `tests/integration/repositories/`. Приёмка: `make up`,
  затем `make test` зелёный; есть тест, что пользователь не видит чужих
  операций; `src/` не изменён. Разбита по объёму на две последовательные
  части: 10a — фикстуры под `finplan_app`, репозитории, Unit of Work,
  `occurred_on` (`done/2026-09-24-repositories-integration-tests.md`), 10b —
  запросы журнала и параметризованная изоляция по разделу 11.4
  (`done/2026-09-24-ledger-queries-isolation-tests.md`).
- [x] **11. Ревью инфраструктуры.** Исполнитель `reviewer`. Разделы 4.1–4.5.
  Предмет — `git diff` подзадач 8–10. Приёмка как у подзадачи 3.
  По объёму (около 3 650 строк) разбита на четыре параллельные задачи
  ревью: 11a — схема, миграции, роли и RLS
  (`2026-09-24-infrastructure-schema-review.md`), 11b — репозитории,
  Unit of Work и запросы (`2026-09-24-infrastructure-data-access-review.md`),
  11c — фикстуры и тесты репозиториев
  (`2026-09-24-infrastructure-repository-tests-review.md`), 11d — тесты
  запросов и изоляции (`2026-09-24-infrastructure-isolation-tests-review.md`).
  Два нарушения в коде исправлены диспетчером, пробелы в тестах закрыты
  подзадачей 11e (`2026-09-24-infrastructure-review-tests.md`).

### entrypoints

- [x] **12a. Парсер быстрого ввода, валидация, форматтеры.** Исполнитель
  `developer`. Разделы 6.5, 6.6, 3.1. `bot/parsers/quickinput.py`,
  `bot/formatters/`. Чистый код без aiogram-хендлеров. Приёмка: `make test` и
  `make lint` зелёные; вне `src/finplan/entrypoints/bot/parsers/` и
  `formatters/` ничего не изменено.
  Файл задачи — `docs/tasks/done/2026-09-24-bot-quickinput-formatters.md`.
- [x] **12b. Сборка бота: container, middleware, онбординг.** Исполнитель
  `developer`. Разделы 6.1–6.4, 12.1, 12.2. `container.py`, middleware
  `Logging`, `User`, `Error`, `states.py`, роутеры `start`
  (онбординг), `common`, `fallback`, регистрация всех роутеров этапа и
  `set_my_commands` в `bot/main.py`. Приёмка: бот стартует на
  `MemoryStorage`; `make test` и `make lint` зелёные. Параллельна 12a: общих
  файлов нет.
  Файл задачи — `docs/tasks/done/2026-09-25-bot-assembly-onboarding.md`.
- [x] **13. Диалоги `/expense` и `/income`.** Исполнитель `developer`.
  Разделы 6.3, 6.6, 6.7. Роутеры `expense`, `income`, клавиатуры категорий и
  счетов. Приёмка: `make test` и `make lint` зелёные; вне `bot/routers/`,
  `bot/keyboards/` и `bot/states.py` ничего не изменено.
  По объёму разбита на две последовательные задачи: 13a — клавиатуры
  выбора, общие функции шагов и диалог расхода
  (`2026-09-25-bot-expense-dialog.md`), 13b — диалог дохода на тех же
  функциях (`2026-09-25-bot-income-dialog.md`). `AddIncome.comment` в
  `bot/states.py` добавил диспетчер вместе с правкой раздела 6.3.
  Файлы задач — `docs/tasks/done/2026-09-25-bot-expense-dialog.md` и
  `docs/tasks/done/2026-09-25-bot-income-dialog.md`.
- [ ] **14. Быстрый ввод, `/balance`, `/today`, `/month`, `/undo`.**
  Исполнитель `developer`. Разделы 6.2, 6.5, 4.2. Роутеры `quick`,
  `accounts`, `reports` и роутер `/undo`, определённый в 6.2 при подзадаче
  12b. Приёмка как у 13. После 13, потому что делит с ней клавиатуры.
- [ ] **15a. Модульные тесты парсера и форматтеров.** Исполнитель `tester`.
  Разделы 11.1, 6.5. Приёмка: `make test` зелёный; `src/` не изменён.
- [ ] **15b. Интеграционные тесты хендлеров.** Исполнитель `tester`.
  Разделы 11.3, 11.4. `Dispatcher.feed_update` с подменённой `BaseSession`
  на PostgreSQL и `MemoryStorage` в `tests/integration/bot/`. Приёмка:
  `make up`, затем `make test` зелёный; покрыты онбординг, оба диалога,
  быстрый ввод, три отчёта и `/undo`. Параллельна 15a.
- [ ] **16. Ревью entrypoints.** Исполнитель `reviewer`. Разделы 2.2, 6.1–6.7,
  12.1. Предмет — `git diff` подзадач 12a–15b. Приёмка как у подзадачи 3,
  плюс проверка, что в хендлерах нет бизнес-логики.

### Сквозная проверка

- [ ] **17. Бот на docker compose.** Исполнитель — главная сессия, файл задачи
  не заводится. Разделы 10.3, 13. Поднять стек, пройти в боте `/start`,
  `/expense`, `/income`, быстрый ввод, `/balance`, `/today`, `/month`,
  `/undo`, сверить остатки с таблицей `transactions`. Приёмка: все команды
  отвечают, остатки в боте совпадают с суммой журнала в базе. Без токена
  бота в `.env` проверка считается пропущенной, а подзадача — невыполненной.
