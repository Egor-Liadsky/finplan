# Журнал разработки finplan

Журнал последних действий по проекту: что сделано, почему выбрано именно так и
что осталось открытым. Нужен, чтобы новая сессия — человека или ИИ-ассистента
— восстанавливала контекст разработки, не восстанавливая его по `git log`:
история коммитов показывает, что изменилось, но не показывает причин и
отвергнутых вариантов.

## Как вести файл

- **Новые записи добавляются сверху**, сразу под этим разделом. Читателю
  достаточно начала файла.
- Одна задача — одна запись. Не запись на каждую правку файла.
- Текст — обычная проза на русском, без сокращений и без отсылок, непонятных
  вне той сессии, где запись появилась.
- Отвергнутый вариант и причина отказа важнее перечня изменённых файлов.
- Когда файл превышает примерно 400 строк, старые записи переносятся в
  `docs/journal/<YYYY>-Q<N>.md`, а здесь остаётся ссылка на архив.

Правила, по которым ассистент обязан пополнять журнал, описаны в `CLAUDE.md`,
раздел «Ход работы».

Шаблон записи:

```markdown
## ГГГГ-ММ-ДД — Краткое название задачи

- **Ветка / коммит:** main / 0000000
- **Этап:** к какому этапу внедрения или разделу архитектуры относится

**Сделано.** Один-два абзаца: что изменилось в системе и зачем.

**Решения.** Что выбрано, какая альтернатива отвергнута и почему.

**Проверка.** Какие команды запускались и чем закончились.

**Открытые вопросы.** Что осталось нерешённым и что это блокирует.

**Следующий шаг.** Одно конкретное действие.
```

---

## 2026-09-25 — Диалог дохода и общий модуль шагов

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх 6241a82
- **Этап:** 1, подзадача 13b плана; разделы 2.1, 6.2, 6.3, 6.6, 6.7 `docs/architecture.md`

**Сделано.** Исполнитель `developer` сначала перенёс `DialogScope`,
функции шагов диалога и их вспомогательные функции из
`bot/routers/expense.py` в новый модуль `bot/routers/ledger_dialog.py`.
Перенос дословный, диспетчер сверил его построчно с прежним
`expense.py`: отличается только docstring модуля. В `expense.py`
остались `EXPENSE_SCOPE`, `router` и семь тонких хендлеров с фильтрами.
Затем исполнитель написал `bot/routers/income.py` по тому же образцу:
`INCOME_SCOPE` собран из `TransactionKind.INCOME`, `CategoryKind.INCOME`,
`AddIncome` и `IncomeCallback`, вход по `/income` и кнопке `inc:new`
сбрасывает текущее состояние, шаг `recurring` не используется. Хендлеры
дохода после замены слов «расход» на «доход» совпадают с хендлерами
расхода строка в строку. На этом подзадача 13 плана закрыта целиком.

**Решения.** Общий модуль вынесен до написания дохода, а не оставлен в
`expense.py`: иначе `income.py` импортировал бы шаги из модуля расхода и
зависел бы от него. В `ledger_dialog.py` нет своего `Router`, фильтры
состояний и фабрик `callback_data` остаются в модулях диалогов.
`bot/keyboards/ledger.py` обобщать не понадобилось: после подзадачи 13a
клавиатуры уже принимают фабрику `callback_data` параметром. Модуль
`ledger_dialog.py` диспетчер дописал в дерево каталогов раздела 2.1,
иначе код разошёлся бы с документом, и пересобрал выжимку `make brief`.
Старая запись журнала от 2026-09-24 о дате операции перенесена в архив
`docs/journal/2026-Q3.md`, иначе журнал превысил бы 400 строк.

Исполнитель уложился в 32 обращения к инструментам при ориентире в
сорок. У 13a было 105 при пределе в шестьдесят. Разница объясняется
тем, что эта задача узкая: один перенос кода и один модуль по готовому
образцу, почти без решений по месту.

**Проверка.** `make lint` зелёный, `make test` — 219 passed; диспетчер
повторил обе команды после исполнителя и после правки документа.
Скрипт исполнителя собрал диспетчер через `build_container` и
`create_dispatcher` на `MemoryStorage`: в `income.router` три
обработчика сообщений и четыре обработчика нажатий. Хендлеры тестами
не покрыты, это подзадача 15b. Живого запуска против Telegram не было.

**Открытые вопросы.** Остаются вопросы из записи о 13a. Ключи создаются
через `uuid4()`, а ADR-010 требует UUIDv7, нужна отдельная задача.
Подзадаче 14 нужно поле `opened_on` в `AccountDTO`, а это правка слоя
`application`, которую границы подзадачи 14 не допускают. Кнопка
«‹ Назад» к верхнему уровню категорий подписана так же, как «‹ Назад»
листания, это проверяется в подзадаче 17.

**Следующий шаг.** Подзадача 14 плана: быстрый ввод, `/balance`,
`/today`, `/month`, `/undo`. До заведения файла задачи решить, куда
относится добавление `opened_on` в `AccountDTO`.

---

## 2026-09-25 — Клавиатуры выбора и диалог расхода

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх 21f8cdb
- **Этап:** 1, подзадача 13a плана; разделы 6.2, 6.3, 6.6, 6.7 `docs/architecture.md`

**Сделано.** Подзадача 13 по объёму разбита на две последовательные:
13a — клавиатуры выбора, общие функции шагов и диалог расхода, 13b —
диалог дохода на тех же функциях. Параллельно их не запустить: 13b
читает результат 13a. Эта запись — о 13a. Исполнитель `developer`
написал `bot/keyboards/ids.py` с короткими идентификаторами для
`callback_data`, `bot/keyboards/ledger.py` с клавиатурами категории,
счёта и подтверждения и фабриками `ExpenseCallback` и `IncomeCallback`,
перенесёнными из `menu.py`, и полный диалог `/expense` в
`bot/routers/expense.py`. Шаги диалога — функции, параметризованные
`DialogScope`: видом операции, группой состояний и фабрикой
`callback_data`. `external_key` — `'bot:'` и первые 32 шестнадцатеричных
знака `sha256` от пользователя, чата, сообщения с карточкой и данных
состояния. Состояние очищается только после успешной записи.

**Решения.** До запуска исполнителя в документ внесено следующее.
Раздел 6.3: подкатегория — не отдельное состояние FSM, а второй
уровень клавиатуры в шаге `category`, и выбрать можно родительскую
категорию целиком. У дохода есть шаг комментария, поэтому в
`AddIncome` добавлено состояние `comment`, в документе и в
`states.py`. Кнопки «Дата» на этапе 1 нет. Проверки даты по
`opened_on` счёта требуют этого поля в `AccountDTO`, а его там нет.
Шаг `recurring` отложен до этапа 4. Раздел 6.7: короткий
идентификатор берётся из последних 8 байт UUID, а не из первых, как
было написано. У UUIDv7 первые 6 байт — метка времени, дефолтное
дерево категорий создаётся одной транзакцией, и первые 8 байт таких
категорий различаются лишь 12 случайными битами. На трёх десятках
категорий это совпадение примерно в десяти процентах случаев.

Решения исполнителя приняты без изменений. В данных FSM хранятся
только примитивы, а списки категорий и счетов запрашиваются заново на
каждом шаге. Кэш в состоянии отвергнут: он устаревает после
архивации в другом диалоге. Пагинация круговая. Округление для
предупреждения в карточке делается в роутере той же политикой, что в
`RecordTransaction`: карточка показывает сумму до подтверждения.
Общие функции шагов исполнитель оставил в `expense.py`, прочитав
границу задачи как запрет на новый модуль в `routers/`. Импорт дохода
из модуля расхода отвергнут, поэтому задание 13b начинается с переноса
этих функций в `bot/routers/ledger_dialog.py`.

**Проверка.** `make lint` зелёный, `make test` — 219 passed, у
исполнителя и повторно у диспетчера. Скрипт исполнителя собрал
диспетчер на `MemoryStorage`: самая длинная `callback_data` — 26 байт
при пределе 64. Два UUID с общими первыми 8 байтами получают разные
короткие идентификаторы. Хендлеры тестами не покрыты, это подзадача
15b. Живой запуск против Telegram не делался.

**Открытые вопросы.** Исполнитель сделал 105 обращений к инструментам.
В задании было правило остановиться на шестидесятом, но исполнитель
его не выполнил. Значит, ориентир в задании объём не ограничивает, и
делить надо мельче: клавиатуры отдельно от роутера. Код расходится с
ADR-010: первичные ключи генерируются `uuid4()`, а не UUIDv7.
Короткие идентификаторы работают при обоих, но расхождение надо
закрыть отдельной задачей, правкой кода или ADR. Подзадаче 14 для
проверки дат быстрого ввода понадобится `opened_on` в `AccountDTO`,
а это правка `application`, выходящая за границы подзадачи 14 в плане.
Кнопка «‹ Назад» к верхнему уровню категорий и «‹ Назад» пагинации
подписаны одинаково. Это заметно, только когда у категории больше
восьми потомков. Проверить стоит в подзадаче 17.

**Следующий шаг.** В новой сессии — подзадача 13b по файлу
`docs/tasks/2026-09-25-bot-income-dialog.md`.

## 2026-09-25 — Сборка бота: контейнер, middleware, онбординг

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх cf0abd0
- **Этап:** 1, подзадача 12b плана; разделы 2.1, 6.2, 6.3, 6.7, 12.2 `docs/architecture.md`

**Сделано.** Бот собран целиком. `src/finplan/container.py` строит
`Container`: engine, фабрику Unit of Work, часы и все восемь use case.
`bot/main.py` получил функцию `create_dispatcher(container, storage)`,
которая подключает `LoggingMiddleware` и `ErrorMiddleware` на уровне
апдейта, `start.router` напрямую, а остальные роутеры — внутри
родительского роутера `registered`, где висит `UserMiddleware`. Модульные
`bot` и `dp` убраны: интеграционным тестам нужна фабрика, а не объекты,
которые строятся при импорте. `/start` проводит нового пользователя через
онбординг в четыре шага и вызывает `RegisterUser`. `common.router`
обрабатывает `/cancel`, `/menu`, навигацию по меню и кнопки
reply-клавиатуры, `fallback.router` — всё остальное. Роутеры подзадач 13
и 14 заведены пустыми и уже подключены, поэтому те подзадачи `main.py`
не трогают. В парсер добавлена `parse_amount` для суммы без знака и
комментария — её берут шаг остатка в онбординге и шаг суммы в диалогах.
`SystemClock` и фабрику `create_bot` с `parse_mode=HTML` диспетчер
написал сам, отдельным коммитом, чтобы не давать исполнителю два слоя.

**Решения.** До запуска исполнителя в документ внесено следующее.
Раздел 6.2: `/undo` получил свой роутер. Порядок middleware изменён,
`ErrorMiddleware` теперь стоит раньше `UserMiddleware`. При прежнем
порядке ошибка базы на поиске пользователя проходила мимо
`ErrorMiddleware`, и бот молчал, а раздел 12.2 этого не допускает.
Отдельный `if user is None` в каждом хендлере отвергнут в пользу
родительского роутера с `UserMiddleware`: проверку легко забыть в новом
хендлере. Раздел 6.3: онбординг — валюта, таймзона кнопкой или именем
IANA, тип первого счёта, остаток. Имя счёта не спрашивается, им
становится подпись кнопки. Отвергнуто брать таймзону из `language_code`:
язык таймзону не определяет, а ошибка в ней сдвигает `occurred_on`.
Раздел 6.7: меню этапа 1 показывает только кнопки, у которых уже есть
обработчик.

При приёмке исполнитель вернул два вопроса. Первый — противоречие в
документе. Раздел 6.3 обещал `/cancel` посреди онбординга, но
`Command('cancel')` был только у `common.router`, куда
незарегистрированный пользователь не проходит. Исправлено: `start.router`
обрабатывает `/cancel` в состояниях `Onboarding`, строка таблицы 6.2
дополнена. Второй — тест этапа 0 `tests/integration/bot/test_start.py`
вызывал `/start` без контейнера. Теперь он проверяет `/help`, которому
база не нужна. `/start` с базой покрывает подзадача 15b.

**Проверка.** `make lint` зелёный, `make test` — 219 passed. Критерий
приёмки, то есть сборка диспетчера через `build_container` и
`create_dispatcher` на `MemoryStorage`, выполнен исполнителем. Живой
запуск против Telegram не делался.

**Открытые вопросы.** Роутеры — модульные объекты. Поэтому
`create_dispatcher` можно вызвать один раз на процесс: повторное
подключение того же роутера aiogram отвергает с `RuntimeError`. В
подзадаче 15b диспетчер понадобится в фикстуре с областью модуля, иначе
придётся перевести роутеры на фабрики. Исполнитель сделал 103 обращения
к инструментам, больше ориентира в семьдесят. Задание такого же объёма
надо было разбить на два: контейнер и middleware отдельно от
онбординга с меню. Тексты подсказок онбординга и меню придуманы
исполнителем. Их список — в файле задачи
`docs/tasks/done/2026-09-25-bot-assembly-onboarding.md`, перечитать их
стоит в подзадаче 17.

**Следующий шаг.** Подзадача 13 — диалоги `/expense` и `/income`.

## 2026-09-24 — Парсер быстрого ввода и форматтеры бота

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх f545f42
- **Этап:** 1, подзадача 12a плана; разделы 6.5, 6.6, 3.1 `docs/architecture.md`

**Сделано.** Исполнитель `developer` написал в `entrypoints/bot/parsers/`
разбор строки быстрого ввода по грамматике 6.5, а в
`entrypoints/bot/formatters/` — форматтеры сумм, дат, остатков, сводок,
карточки операции, сторно и словарь текстов ошибок и предупреждений.
Весь этот код чистый: aiogram он не импортирует, базы и часов не знает.
Хендлеры подключат его в подзадачах 13 и 14. Тестов пока нет, их пишет
`tester` в подзадаче 15a.

**Решения.** До запуска исполнителя в раздел 6.6 добавлены два абзаца.
Первый задаёт границу между парсером и хендлером. Парсер проверяет
только то, что видно из самой строки, и возвращает сумму без
округления. Округление до `minor_unit`, проверку даты относительно
`opened_on` и запрет даты позже завтрашней выполняет хендлер: парсер не
знает, какой счёт выберет пользователь, а значит, не знает и валюты.
Вариант передавать счёт в парсер отвергнут. Счёт разрешается уже после
разбора — по `@`, по счёту по умолчанию или отдельным шагом диалога, —
так что парсер пришлось бы вызывать дважды. Трёхбуквенное слово, которого
нет в справочнике валют, считается словом комментария, а не ошибкой:
иначе «-50 bar» отклонялось бы. Второй абзац закрепляет
`parse_mode=HTML` с экранированием пользовательского текста через
`html.escape` и один словарь текстов в `formatters/errors.py`. Парсер и
хендлеры возвращают коды, а не фразы.

При приёмке диспетчер исправил две вещи. Регулярное выражение суммы не
требовало границы после числа, и «1 2000» разбиралось как 1200 с
комментарием «0». Теперь сумма обязана кончаться пробелом или концом
строки. Текст ошибки диапазона «Сумма должна быть больше нуля»
показывался и для суммы от 1e12, поэтому текст и строка таблицы 6.6
стали такими: «Сумма должна быть больше нуля и меньше триллиона».

**Проверка.** `make test` — 219 passed, `make lint` зелёный. Пять
примеров из таблицы 6.5 и около двадцати граничных строк прогнаны
вручную через `uv run python`.

**Открытые вопросы.** Тексты ошибок, для которых в 6.6 нет готовой
формулировки, исполнитель написал по смыслу. Их стоит перечитать при
сквозной проверке в подзадаче 17.

**Следующий шаг.** Подзадача 12b. До запуска исполнителя закрыть в
разделах 2.1, 6.2 и 6.3 пробелы про роутер `/undo`, `common.py` и
`fallback.py`, а также состояния онбординга.

## 2026-09-24 — Ревью инфраструктуры этапа 1 и исправления по нему

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх 540d38c
- **Этап:** 1, подзадача 11 плана; разделы 4.1–4.5, 5.7, 3.3, 11.4 `docs/architecture.md`

**Сделано.** Исполнитель `reviewer` проверил всё, что появилось в
подзадачах 8–10: диапазон `e3aad34..540d38c`, около 3 650 строк. Из-за
объёма ревью разделено на четыре задачи, запущенные одновременно: схема
и миграции (11a), репозитории, Unit of Work и запросы (11b), фикстуры и
тесты репозиториев (11c), тесты запросов и изоляции (11d). В коде
подтвердились два дефекта, и оба исправлены. Первый: остаток по счёту
не учитывал операции `kind = interest`, хотя формула 5.7 их включает.
На этапе 1 таких операций нет, но с вкладами этапа 2 остатки
занижались бы. Второй: при нарушении ключа `uq_transactions_reverses_id`,
то есть при повторном сторно, репозиторий поднимал `DuplicateError` с
текстом про `external_key`. Теперь он различает ключ по
`constraint_name` исключения asyncpg. Обе правки мелкие, поэтому их
сделала главная сессия, без отдельного файла задачи. Тесты на них и на
пробелы, найденные в 11c и 11d, дописал исполнитель `tester` в
подзадаче 11e. Добавлены: таймзона с отрицательным смещением от UTC на
трёх уровнях, повторное сторно, `interest` в остатке, суммирование
`base_amount` при валюте счёта, отличной от базовой, и отказ триггера
`trg_transactions_immutable` на `UPDATE` и `DELETE` с `SQLSTATE P0001`.
Для тестов на обе правки исполнитель подтвердил, что при временном
откате кода они падают.

**Решения.** Нарушение из 11a отклонено как ложное. Ревьюер строил DDL
миграции статически и получил короткие имена CHECK-ограничений.
Запрос к `pg_constraint` работающей базы показал полные имена вида
`ck_transactions_transfer_shape`: Alembic в `op.create_table` берёт
`naming_convention` из `target_metadata`. Замечание 11b об Unit of Work
после `rollback()` отложено. Явный `rollback()` сейчас не вызывается, а
продолжение без `app.user_id` под RLS даёт пустой результат, а не чужие
данные. Вернуться к нему нужно вместе с первым use case, который
откатывает транзакцию явно. Флаг смешанной базовой валюты в сводках
отложен до этапа 2: смены `User.base_currency` на этапе 1 нет.
`adjustment` в остатке по-прежнему не учитывается, потому что поля
направления корректировки из 5.7 в журнале нет; это записано в
докстроке запроса.

**Проверка.** `uv run pytest -q` — 219 passed. `make lint` зелёный.

**Открытые вопросы.** Поведение Unit of Work после явного `rollback()` —
см. «Решения».

**Следующий шаг.** Подзадача 12a плана: парсер быстрого ввода,
валидация и форматтеры бота — исполнитель `developer`, в новой сессии.

---

## 2026-09-24 — Интеграционные тесты репозиториев, запросов и изоляции

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх d4b6d7d
- **Этап:** 1, подзадача 10 плана; разделы 11.1, 11.4, 4.5, 4.3 `docs/architecture.md`

**Сделано.** Слой доступа к данным этапа 1 впервые проверен против
настоящей PostgreSQL 16 из `testcontainers`: в
`tests/integration/repositories/` появились 48 тестов. Они проверяют, что
четыре репозитория записывают и читают сущности без потери точности
`Decimal`, что `DuplicateError` возникает на уникальных ключах и что
Unit of Work соблюдает свой контракт: `commit`, откат без `commit`,
запрет повторного `commit`. Отдельный тест подтверждает, что операция в
22:30 UTC у пользователя из Москвы получает `occurred_on` следующего
дня. Запросы `account_movements` и `totals_by_category` проверены на
точных суммах, на исключении сторнированных пар и на границах
полуинтервала `[start, end)`. Изоляция по разделу 11.4 — это
параметризованный тест. Он сам собирает все публичные методы
репозиториев и запросов журнала и падает, если для нового метода не
описана проверка. Ещё один тест показывает, что прямой `SELECT` без
фильтра по `user_id` под ролью `finplan_app` отсекает чужие строки уже
на уровне RLS. Дефектов продуктового кода тесты не нашли; `src/` не
менялся.

Работу выполнил исполнитель `tester` в двух последовательных запусках.
10a — фикстуры, репозитории, Unit of Work
(`docs/tasks/done/2026-09-24-repositories-integration-tests.md`). 10b —
запросы журнала и изоляция
(`docs/tasks/done/2026-09-24-ledger-queries-isolation-tests.md`). Первый
запуск оборвался из-за сетевой ошибки API; его возобновили в том же
экземпляре, а не запускали заново.

**Решения.** Тесты работают под ролью `finplan_app`, а не под
владельцем схемы: владелец обходит RLS. Сессии открываются через
`join_transaction_mode="create_savepoint"` внутри внешней транзакции,
которая никогда не коммитится. Отсюда главное решение по фикстурам:
владелец данных и «сосед» в тестах изоляции работают на одном
соединении через общую фикстуру `uow_factory`. Вариант с раздельными
соединениями отвергнут: `commit()` там — лишь `RELEASE SAVEPOINT`,
данные владельца другому соединению не видны при `READ COMMITTED`, и
тест изоляции проходил бы, даже если RLS не работает. Значение
`app.user_id` живёт до конца внешней транзакции, а не `SAVEPOINT`. Для
Unit of Work с известным пользователем это безопасно: каждый вход
выставляет значение заново. Для единственного случая без пользователя —
`get_by_telegram_id` — фикстура `reset_app_user_id` сбрасывает значение
явно. Проверка изоляции распространена и на методы записи: попытка
соседа вставить строку с чужим `user_id` должна давать отказ RLS с кодом
`42501`. Иначе `add` и `add_many` остались бы методами без проверки.
`get_by_telegram_id` намеренно не изолирован по Unit of Work: по
разделу 4.5 он идёт через `SECURITY DEFINER`-функцию до
аутентификации. Это записано в тесте, а не пропущено молча.

Попутно из этого файла в `docs/journal/2026-Q3.md` перенесены две самые
старые записи: файл превысил 400 строк.

**Проверка.** `uv run pytest -q` — 210 passed, из них 48 в
`tests/integration/repositories`. `make lint` зелёный. `git diff --stat
-- src migrations` пуст.

**Открытые вопросы.** Нет.

**Следующий шаг.** Подзадача 11 плана — ревью инфраструктуры
исполнителем `reviewer` по `git diff` подзадач 8–10, в новой сессии.

---

## Архив

Записи, вытесненные из этого файла по правилу объёма, лежат в
[`docs/journal/2026-Q3.md`](journal/2026-Q3.md).
