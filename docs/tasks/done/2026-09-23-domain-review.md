# Ревью доменного ядра этапа 1 и его модульных тестов

- **Исполнитель:** reviewer
- **Слой:** domain
- **Раздел архитектуры:** docs/architecture.md, разделы 2.2, 3.1, 3.2, 3.3, 3.4, 4.2, 11.1, 11.2
- **Заведена:** 2026-09-23

## Входные условия

Это подзадача 3 плана `docs/tasks/2026-09-23-stage1-plan.md`. Закрыты две
предыдущие подзадачи:

- подзадача 1, коммит `538213f`: доменное ядро в `src/finplan/domain/common/`
  (`currency.py`, `money.py`, `period.py`, `errors.py`) и
  `src/finplan/domain/entities/` (`user.py`, `account.py`, `category.py` с
  `DEFAULT_CATEGORY_TREE`, `transaction.py` с правилом сторно). Тем же
  коммитом в раздел 3.3 документа добавлен состав дефолтного дерева
  категорий. Задание и отчёт исполнителя —
  `docs/tasks/done/2026-09-23-domain-core.md`;
- подзадача 2, коммит `042b449`: 98 модульных тестов в `tests/unit/domain/`.
  Задание и отчёт — `docs/tasks/done/2026-09-23-domain-unit-tests.md`.

`make test` (108 passed) и `make lint` зелёные на `042b449`.

Предмет ревью — дифф кода и тестов двух коммитов, около 1580 строк в 16
файлах:

```bash
git diff --stat e851540..042b449 -- src tests
git diff e851540..042b449 -- src/finplan/domain/common
git diff e851540..042b449 -- src/finplan/domain/entities
git diff e851540..042b449 -- tests/unit/domain
```

Дифф читать по каталогам, как выше, а крупные файлы (`category.py`,
`transaction.py`, `test_transaction.py`) — с отсечением через `sed -n`,
а не одним выводом на весь предмет.

До начала ревью прочитать:

- `docs/architecture-brief.md` целиком — дословные правила слоёв, денег и
  тестов;
- `docs/architecture.md` через `Read` с `offset` и `limit`: раздел 2.2 —
  строки 377–392; 3.1 — 395–443; 3.2 — 444–466; 3.3 — 467–744 (там же состав
  дефолтного дерева, добавленный в `538213f`); 3.4 — 745–785; 4.2 — 809–833;
  11.1 и 11.2 — 2732–2775. Если номера строк разошлись с картой в выжимке,
  верна карта в выжимке.

Отчёты исполнителей в `docs/tasks/done/` читать только при необходимости
понять, почему выбрано так, а не иначе, — предмет ревью код, а не отчёты.

## Задание

Проверить дифф по списку «Что искать» из роли `reviewer` с упором на домен:

- `domain` не импортирует `application`, `infrastructure`, `entrypoints`,
  SQLAlchemy, aiogram, FastAPI, pydantic и не делает IO, включая чтение
  системных часов и `datetime.now()` без явно переданного момента;
- деньги — только `Decimal` внутри `Money`; `float` нигде не принимается и не
  возвращается, включая тесты; округление и `minor_unit` по 3.1;
- `Period` — полуинтервал `[from, to)` по всему коду, в том числе в
  `for_day` и `for_month`;
- состав полей, типы и инварианты сущностей `User`, `Account`, `Category`,
  `Transaction` и перечисления совпадают с разделами 3.2 и 3.3; связи — с 3.4;
- сторно соответствует разделу 4.2: проведённая операция не изменяется
  иначе, чем переходом в `reversed`, отмена — только новой записью;
- дефолтное дерево категорий в коде совпадает с составом в разделе 3.3
  поимённо, а не только по количеству;
- тесты проверяют правила документа, а не повторяют реализацию; нет тестов,
  которые проходят при любом поведении кода; нет пропусков относительно
  11.1 и 11.2 там, где домен этапа 1 их требует.

Каждую находку отнести к одному из уровней:

- **нарушение** — противоречит `docs/architecture.md` или правилу слоёв,
  блокирует отметку подзадачи;
- **замечание** — поведение корректно по документу, но есть реальный риск
  дефекта на следующих подзадачах этапа 1 (application, infrastructure);
  назвать, на какой подзадаче он проявится;
- **к документу** — код прав, а документ устарел или молчит; адресуется
  диспетчеру для правки документа.

Известный и уже учтённый пробел не повторять находкой: в таблице
`categories` раздела 4.3 нет столбца `aliases`; он закрывается до подзадачи 8.

## Границы

- правится только раздел `## Результат исполнителя` в этом файле; `src/`,
  `tests/`, `docs/architecture.md`, `docs/dev-log.md` и остальные файлы
  `docs/` не изменяются — проверяется `git status --short`;
- тесты и линтеры не запускаются: они зелёные на `042b449`, это проверено
  диспетчером; если находке нужен прогон, так и написать;
- ревью не выходит за файлы из `git diff --stat e851540..042b449 -- src tests`;
  соседний код читается только для контекста;
- вкусовщина без ссылки на правило документа в список не попадает.

## Критерий приёмки

- в конце файла есть раздел `## Результат исполнителя` из четырёх подпунктов
  по `docs/tasks/README.md`;
- каждая находка содержит `file:line`, существующий в `042b449`, уровень из
  трёх названных, нарушенное правило с номером раздела документа и
  последствие;
- если находок какого-либо уровня нет, это сказано прямым текстом;
- `git status --short` показывает изменённым только этот файл.

## Результат исполнителя

**Сделано.** Проверен дифф `e851540..042b449` по каталогам
`src/finplan/domain/common`, `src/finplan/domain/entities`,
`tests/unit/domain` (16 файлов, ~1580 строк): `currency.py`, `errors.py`,
`money.py`, `period.py`, `account.py`, `category.py`, `transaction.py`,
`user.py` и парные тестовые файлы. Против правил из
`docs/architecture-brief.md` (2.2, 3.1, 4.1, 11.1, 11.4) и разделов
`docs/architecture.md` 3.2, 3.3, 3.4, 4.2, 11.2 проверены: направление
зависимостей и отсутствие IO/`float`/запрещённых импортов (по `grep` и по
чтению кода), полуинтервал `Period`, состав полей и инвариантов `User`,
`Account`, `Category`, `Transaction`, поимённое совпадение
`DEFAULT_CATEGORY_TREE` с таблицей 3.3, правило сторно из 4.2/3.3, качество
тестов по 11.1/11.2. Найдено: **нарушений — 0, замечаний — 1, находок «к
документу» — 0**.

**Проверка.**

- Нарушений нет.
- Находок «к документу» нет.
- Замечание: `src/finplan/domain/entities/category.py:32` (класс `Category`,
  `__post_init__` на строке 45) не проверяет и структурно не может проверить
  инвариант раздела 3.3 «запрещены циклы» — у одной сущности `Category` нет
  доступа к цепочке предков, кроме прямого `parent_id`. Поведение
  корректно: цикл через один `parent_id` уровня и так исключён проверками
  `depth`/`new_child`, но защита от цикла через перенос поддерева (когда
  категории меняют `parent_id` местами) домену недоступна в принципе.
  Риск — на подзадаче use case создания/переноса категории в `application`
  (стадия 1, работа с `CategoryRepository`): если там не добавить обход
  цепочки предков перед сохранением нового `parent_id`, цикл проскочит в
  БД. Тестов на это в `tests/unit/domain` соответственно тоже нет — и не
  должно быть на этом слое, тест такого инварианта относится к
  application/integration уровню.
- Не хватает: прогона `make test`/`make lint` не делал — по условию задачи
  диспетчер уже подтвердил их зелёный статус на `042b449`, повторный запуск
  не требовался.

**Решения и отложенное.** Не стал заводить находки по уникальности имени
счёта среди неархивных (`Account`, раздел 3.3), уникальности имени и `path`
категории среди сиблингов/пользователя и уникальности `external_key`
операции — все три инварианта явно относятся к схеме БД (уникальные
индексы, раздел 4.1/4.3) и заведомо реализуются позже, на подзадачах
infrastructure; в отличие от «циклов» они не требуют логики обхода графа и
типовая работа `UNIQUE`-ограничения их закрывает без риска забыть, поэтому
замечанием не оформлял. Не проверял отчёты `docs/tasks/done/` — предмет
ревью код и тесты, а не обоснования выбора.

**Вопросы диспетчеру.** Нет.
