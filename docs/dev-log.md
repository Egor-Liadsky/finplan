# Журнал разработки finplan

Журнал последних действий по проекту: что сделано, почему выбрано именно так и
что осталось открытым. Нужен, чтобы новая сессия — человека или ИИ-ассистента
— восстанавливала контекст разработки, не восстанавливая его по `git log`:
история коммитов показывает, что изменилось, но не показывает причин и
отвергнутых вариантов.

## Как вести файл

- **Новые записи добавляются сверху**, сразу под этим разделом. Читателю
  достаточно начала файла.
- Одна задача — одна запись. Не запись на каждую правку файла.
- Текст — обычная проза на русском, без сокращений и без отсылок, непонятных
  вне той сессии, где запись появилась.
- Отвергнутый вариант и причина отказа важнее перечня изменённых файлов.
- Когда файл превышает примерно 400 строк, старые записи переносятся в
  `docs/journal/<YYYY>-Q<N>.md`, а здесь остаётся ссылка на архив.

Правила, по которым ассистент обязан пополнять журнал, описаны в `CLAUDE.md`,
раздел «Ход работы».

Шаблон записи:

```markdown
## ГГГГ-ММ-ДД — Краткое название задачи

- **Ветка / коммит:** main / 0000000
- **Этап:** к какому этапу внедрения или разделу архитектуры относится

**Сделано.** Один-два абзаца: что изменилось в системе и зачем.

**Решения.** Что выбрано, какая альтернатива отвергнута и почему.

**Проверка.** Какие команды запускались и чем закончились.

**Открытые вопросы.** Что осталось нерешённым и что это блокирует.

**Следующий шаг.** Одно конкретное действие.
```

---

## 2026-09-24 — Ревью слоя application этапа 1 и исправление находок

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх 2b5cb31
- **Этап:** 1, подзадача 7 плана; разделы 2.2, 3.1, 3.3, 4.2, 4.5, 11.1, 13 `docs/architecture.md`

**Сделано.** Проверен весь слой application этапа 1: порты, DTO, восемь use
case, правки документа и модульные тесты подзадач 4–6. Предмет ревью —
около 2 750 строк, заметно больше ориентира на один запуск исполнителя.
Поэтому ревью разбито на два параллельных запуска `reviewer`: код с
правками документа и отдельно тесты. Файлы задач —
`docs/tasks/done/2026-09-24-application-code-review.md` и
`docs/tasks/done/2026-09-24-application-tests-review.md`. Находки уровня
«нарушение» закрыты ещё двумя подзадачами, запущенными последовательно:
`developer` исправил код
(`docs/tasks/done/2026-09-24-application-review-fixes.md`), затем `tester`
дописал тесты (`docs/tasks/done/2026-09-24-application-review-tests.md`).
Тестам нужен был уже исправленный код, поэтому одновременно их не
запускали.

Исправления в коде. `RecordTransaction` теперь округляет сумму до
`minor_unit` валюты по `ROUND_HALF_UP`, как требует таблица округления
раздела 3.1, и кладёт то же значение в `base_amount`. Если сумма после
округления стала нулём, use case бросает `InvalidCommandError`.
`RegisterUser` тем же правилом округляет начальный остаток счёта; нулевой
остаток допустим. Неизвестная или синтаксически неверная таймзона даёт
`InvalidCommandError`, а не `ZoneInfoNotFoundError` или `ValueError`, и
тест с `xfail(strict=True)` лишился пометки. Существующего пользователя
`RegisterUser` теперь ищет в отдельной транзакции с `user_id = None` и
только потом создаёт нового от имени сгенерированного `id`. Раньше поиск
шёл в транзакции от имени ещё не созданного пользователя: под RLS он бы
промахивался, и каждый повторный `/start` шёл бы через неудачную вставку.
Ветка гонки двух `/start` через `DuplicateError` сохранена. Новые тесты
покрывают пропуски, найденные ревью, — категорию чужого вида,
`NotFoundError` у `GetBalances` и `GetPeriodSummary`, границу полуинтервала
для месяца в таймзоне, отличной от UTC, — и всё новое поведение.

**Решения.** Проверку таймзоны положили в use case, а не в валидатор DTO.
Раздел 2.2 относит команду, противоречащую данным, к `InvalidCommandError`
сценария, и рядом уже стоит такая же проверка кода валюты. Валидатор
pydantic дал бы `ValidationError` с другим путём обработки. Нулевая после
округления сумма операции отвергается в use case, а не доменным
инвариантом. Так пользователь получает ошибку сценария с понятным текстом,
а не доменное исключение о неположительной сумме.

Одну находку ревью кода главная сессия отклонила. Ревью указало, что
операции с `occurred_at` в будущем создаются как `posted`, хотя раздел 3.3
требует для них `status = pending`. Но `pending`-операции и задача
`post_pending` по разделу 13 входят в этап 4. На этапе 1 операция с датой
дальше чем на сутки вперёд отвергается доменным инвариантом, и этого
достаточно. Проверку с валютой, у которой `minor_unit = 0`, тесты не
содержат: в заглушках есть только RUB, USD и EUR. Добавлять JPY ради
одного теста не стали, правило округления и так проверено через
`Money.round_to_minor_unit` в доменных тестах.

Стоимость. Два ревью — 41 и 47 обращений к инструментам, около 91 и
93 тысяч токенов. Исправление кода — 22 обращения и 60 тысяч, тесты —
40 обращений и 85 тысяч. Все запуски уложились в ориентир около семидесяти
обращений. Разбиение ревью по объёму себя оправдало: один запуск на весь
предмет вышел бы примерно на 90 обращений, а его стоимость растёт как
квадрат их числа.

**Проверка.** Главная сессия повторила проверки после каждого
исполнителя. На `2b5cb31` `make lint` зелёный, `make test` —
149 passed, пометок `xfailed` нет. Исполнитель тестов отдельно прогнал
`uv run mypy --strict tests/unit/application` — чисто.
`grep -rn "float" tests/unit/application` пуст.

**Открытые вопросы.** Политика RLS для таблицы `users` в разделе 4.5 не
описана. `RegisterUser` и `FindUserByTelegramId` ищут пользователя по
`telegram_id` в транзакции без `app.user_id`. Если на `users` включить
ту же политику `id = current_setting('app.user_id')`, что на остальных
таблицах, поиск перестанет находить кого-либо. Решение нужно принять в
разделе 4.5 до подзадачи 8. Варианты: не включать RLS на `users`, открыть
политикой `SELECT` при пустом `app.user_id` или искать отдельной ролью.
Прежние вопросы остаются: столбец `aliases` в `categories` — до
подзадачи 8; `mypy` для `tests/` в `make lint` — отдельно.

**Следующий шаг.** Подзадача 8 в новой сессии. Перед запуском `developer`
главная сессия фиксирует в разделе 4.5 политику RLS для `users`, а в
разделах 3.3 и 4.3 — столбец `aliases`.

---

## 2026-09-23 — Модульные тесты use case этапа 1

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх a981455
- **Этап:** 1, подзадача 6 плана; разделы 11.1, 2.1, 2.2, 3.3, 4.2 `docs/architecture.md`

**Сделано.** Все восемь use case этапа 1 покрыты модульными тестами на
in-memory реализациях портов — уровень «модульные с заглушками» из раздела
11.1. Перед запуском исполнителя главная сессия добавила каталог
`tests/unit/application/` в дерево раздела 2.1 и пересобрала выжимку. Тесты
написал исполнитель `tester` по файлу задачи
`docs/tasks/done/2026-09-23-application-use-case-tests.md`. Заглушки лежат в
`tests/unit/application/fakes.py`: одно общее хранилище на фабрику Unit of
Work, у каждой транзакции своя рабочая копия, которая применяется только по
`commit`. Фабрика запоминает, с каким `user_id` открывалась каждая
транзакция и закоммичена ли она. Поэтому тесты проверяют и то, что при
ошибке сценария ничего не записано, и то, что транзакция открыта от имени
нужного пользователя. `LedgerQueries` считает остатки и итоги из тех же
хранимых операций, а не из подложенных чисел. Гонку двух `/start`
изображает крючок в хранилище: встречная транзакция будто бы коммитится
ровно в момент `UserRepository.add`. Настоящей многопоточности в модульных
тестах нет, а ветку `except DuplicateError` крючок проверяет честно.

**Решения.** В задании главная сессия ошиблась: написала, что сторнирующая
операция «имеет противоположную сумму». Исполнитель заметил, что это
противоречит разделу 3.3, абзацу «Форма сторно»: сторнирующая запись
повторяет `amount` исходной и сразу получает `status = reversed`. Пара
выпадает из агрегатов по статусу, потому что они считают только `posted`,
а не за счёт противоположных знаков. Код, документ и доменные тесты между
собой согласны, поэтому тест написан по документу, без `xfail`, а текст
задания признан ошибочным. Это ответ на вопрос, который исполнитель оставил
в файле задачи. Сам файл задачи после запуска не правится по протоколу
`docs/tasks/README.md`. Известное расхождение с таймзоной оформлено тестом
с `xfail(strict=True)`: неизвестная таймзона в `RegisterUserCommand` даёт
`ZoneInfoNotFoundError` вместо `InvalidCommandError`. Когда код починят,
тест начнёт проходить и заставит снять пометку.

Протокол этот запуск нарушил: при ориентире около семидесяти обращений
исполнитель сделал 93 и потратил 176 тысяч токенов. Задачу не разбили,
потому что оценка по объёму кода — около 460 строк use case и пять портов —
обещала меньше. Недооценён был сам тестовый код: 1 625 строк против 457
строк продуктового. Для следующих задач `tester` верная оценка — примерно
втрое больше строк, чем в покрываемом коде. Задачу такого размера стоило
делить на две: заглушки с тестами записи и тесты чтения.

**Проверка.** Главная сессия повторила проверки после исполнителя.
`make lint` зелёный, `mypy` без ошибок на 79 файлах. `make test` —
136 passed, 1 xfailed. Исполнитель отдельно прогнал
`uv run mypy --strict tests/unit/application` — чисто. Это и подтверждает,
что заглушки структурно соответствуют протоколам портов.
`grep -rn "float" tests/unit/application` пуст, `src/` не изменён.

**Открытые вопросы.** `make lint` проверяет `mypy` только `src`. Значит,
соответствие заглушек протоколам никто не проверяет автоматически, и при
изменении порта тесты молча разойдутся с ним. Включать ли `tests/` в `mypy`
в `make lint`, решить отдельно: это меняет `Makefile` и, возможно, требует
аннотаций в старых тестах. Прежние открытые вопросы остаются: таймзона —
в ревью подзадачи 7, столбец `aliases` — до подзадачи 8.

**Следующий шаг.** Подзадача 7 в новой сессии: `reviewer` читает `git diff`
подзадач 4–6 по разделам 2.2 и 4.2.

## 2026-09-23 — Use case записи и чтения этапа 1

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх 523db8b
- **Этап:** 1, подзадачи 5a и 5b плана; разделы 2.1, 2.2, 3.3, 4.2, 6.1, 12.3 `docs/architecture.md`

**Сделано.** В слое `application` появились восемь use case этапа 1.
Запись: `RegisterUser` (онбординг с первым счётом и дефолтным деревом
категорий), `FindUserByTelegramId` (для `UserMiddleware`),
`RecordTransaction` (расход или доход) и `UndoLastTransaction` (сторно
последней операции источника). Чтение: `ListAccounts` и `ListCategories`
для клавиатур диалогов, `GetBalances` для `/balance`, `GetPeriodSummary`
для `/today` и `/month`. Код написали два исполнителя `developer`,
запущенные одновременно, по файлам задач
`docs/tasks/done/2026-09-23-application-write-use-cases.md` и
`docs/tasks/done/2026-09-23-application-read-use-cases.md`. Тестов use case
пока нет: это подзадача 6.

**Решения.** До запуска исполнителей главная сессия сделала две вещи сама,
чтобы параллельные задачи не пересеклись в одних файлах и не решали
архитектурные вопросы по месту. Первое — ошибки сценариев вынесены в новый
модуль `application/errors.py`: `NotFoundError` (объект не найден у
пользователя или архивирован) и `InvalidCommandError` (команда противоречит
данным), общий предок `ApplicationError`. Отвергнуты два других места:
`domain/common/errors.py` — «не найдено» не доменный инвариант, а
`ports/repositories.py` — там живут контракты портов, а не отказы
сценариев. Правило записано в разделе 2.2, файл добавлен в дерево 2.1.
Второе — перевод сущностей в DTO сделан методами `from_entity` у
`UserDTO`, `AccountDTO`, `CategoryDTO` и `TransactionDTO`; иначе обе
задачи завели бы собственные функции перевода счёта или правили бы общий
файл одновременно.

Решения, записанные в задания. Регистрация идемпотентна: найденный по
`telegram_id` пользователь или `DuplicateError` от параллельного `/start`
приводят к возврату существующего пользователя с первым неархивным счётом
и `created=False`; данные счетов читаются в отдельной транзакции от имени
найденного `user_id`, потому что RLS покажет только его строки.
Исполнитель добавил к этому повторный поиск через `uow_factory(None)` в
ветке гонки, где найденного пользователя ещё нет в руках, — принято.
Поиска `Currency` по коду в домене нет, поэтому код валюты переводится
словарём констант `RUB`, `USD`, `EUR` прямо в `register_user.py`;
выносить поиск в домен отложено до появления курсов на этапе 2.
`RecordTransaction` отклоняет счёт в валюте, отличной от базовой, и
переводы: курсов и переводов на этапе 1 нет. Сводка за период сворачивает
суммы до корневых категорий и учитывает архивные категории, иначе операции
по ним выпали бы из итога.

**Проверка.** `make lint` зелёный, включая `ruff format --check` и сверку
выжимки; `mypy` без ошибок на 79 файлах. `make test` — 108 passed.
`grep -rn "float" src/finplan/application/use_cases` пуст.

**Открытые вопросы.** Неизвестная таймзона в `RegisterUserCommand`
сейчас даёт `ZoneInfoNotFoundError` из стандартной библиотеки, а не
`InvalidCommandError`; стоит отметить в ревью подзадачи 7. Столбец
`aliases` в таблице `categories` по-прежнему нужно добавить до
подзадачи 8.

**Следующий шаг.** Подзадача 6: исполнитель `tester` пишет in-memory
реализации портов и тесты всех восьми use case в `tests/unit/application/`.

## 2026-09-23 — Порты и DTO слоя application для этапа 1

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх 15fd022
- **Этап:** 1, подзадача 4 плана; разделы 2.2, 3.3, 4.2, 4.5, 6.2, 8.5 `docs/architecture.md`

**Сделано.** В слое `application` появились протоколы портов и DTO всех
use case этапа 1. Реализаций портов и самих use case пока нет. В
`ports/clock.py` лежит `Clock`. В `ports/repositories.py` — протоколы
репозиториев `User`, `Account`, `Category` и `Transaction`, протокол
чтения журнала `LedgerQueries`, запись `CategoryTotal` и исключение
`DuplicateError`. В `ports/uow.py` — `UnitOfWork` и `UnitOfWorkFactory`.
В `dto/` шесть файлов: базовая модель `Dto` и DTO групп `auth`,
`accounts`, `categories`, `transactions`, `reports`. Код написал
исполнитель `developer` по файлу задачи
`docs/tasks/done/2026-09-23-application-ports-dto.md`.

**Решения.** До запуска исполнителя главная сессия устранила противоречие
в документе. По разделу 2.2 транзакцию открывает use case, а раздел 6.2
поручал это `UnitOfWorkMiddleware` на весь апдейт. Выбран use case. Он
получает `UnitOfWorkFactory`, открывает `async with factory(user_id)` и
явно вызывает `commit`. Middleware из раздела 6.2 убран, а `UserMiddleware`
ищет пользователя через use case, а не через репозиторий. Транзакцию на
апдейт отвергли по двум причинам. Во-первых, тогда граница транзакции
лежала бы в `entrypoints`, и тот же use case, вызванный из API или
`worker`, вёл бы себя иначе. Во-вторых, `commit` оказался бы вне use case,
который отвечает за инвариант. Новый абзац раздела 2.2 «Контракт портов и
граница транзакции» фиксирует и остальные правила. Фабрика принимает
`user_id` ради RLS; `None` допустим только при поиске пользователя по
`telegram_id`. Агрегаты журнала читаются через `uow.ledger`, в той же
сессии, и учитывают только операции `posted`: так пара «исходная запись и
сторно» в отчёты не попадает. Нарушение уникального ключа приходит как
`DuplicateError`, чтобы исключение драйвера БД не выходило из
`infrastructure`. Идентификаторы создаются через `uuid.uuid4` без
отдельного порта. DTO работают в строгом режиме pydantic, поэтому `float`
не может молча превратиться в `Decimal`. Отдельный порт запросов вне
`UnitOfWork` отвергли: тогда чтение шло бы в другой сессии и без
`app.user_id`. Первоначальный план тоже поправлен. Middleware
`UnitOfWork` из подзадачи 12b убран. В 5a добавлен поиск пользователя по
`telegram_id`, а в 5b — списки счетов и категорий, без которых не на чем
строить клавиатуры диалогов. Тестов в этой подзадаче нет: in-memory
реализации портов и тесты use case — подзадача 6.

**Проверка.** Исполнитель прогнал `ruff check`, но не проверку формата, и
`make lint` упал на `ruff format --check` в `ports/repositories.py`.
Главная сессия отформатировала файл командой `ruff format`, после чего
`make lint` зелёный, включая `mypy` на 70 файлах. `make test` — 108
passed. `grep -rn "float" src/finplan/application` пуст. Создание
`RecordTransactionCommand` с `amount=1.5` падает с `ValidationError`, а с
`Decimal("1.5")` проходит.

**Открытые вопросы.** В таблице `categories` раздела 4.3 по-прежнему нет
столбца `aliases`. Его нужно добавить до подзадачи 8.

**Следующий шаг.** Подзадачи 5a «Use case записи» и 5b «Use case чтения»
исполнителями `developer`, одним сообщением в новой сессии.

## 2026-09-23 — Ревью доменного ядра этапа 1

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх 042b449
- **Этап:** 1, подзадача 3 плана; разделы 2.2, 3.1–3.4, 4.2, 11.1, 11.2 `docs/architecture.md`

**Сделано.** Исполнитель `reviewer` проверил код и тесты двух предыдущих
подзадач этапа 1: доменное ядро из коммита `538213f` и 98 модульных тестов
из `042b449`, всего 16 файлов. Задание и полный отчёт лежат в
`docs/tasks/done/2026-09-23-domain-review.md`. Нарушений правил документа не
найдено. Домен не импортирует внешние слои и библиотеки, не читает часы и
не использует `float`. `Money` округляет по `ROUND_HALF_UP`, `Period` —
полуинтервал, поля и инварианты сущностей совпадают с разделом 3.3, сторно
соответствует разделу 4.2, дефолтное дерево категорий совпадает с таблицей
3.3 поимённо. Ревьюер нашёл одно замечание: инвариант «запрещены циклы» из
раздела 3.3 сущность `Category` проверить не может, потому что видит только
прямого родителя, а не цепочку предков.

**Решения.** Замечание закрыто правкой раздела 3.3, а не кодом. Там теперь
сказано, что запрет циклов проверяет use case переноса категории в
`application`. Проверка идёт по материализованному пути: `path` нового
родителя не должен совпадать с `path` переносимой категории и не должен
начинаться с него и точки. Обход цепочки предков через репозиторий
отвергнут: `path` уже хранит всю цепочку, и лишние запросы к базе ничего не
дают. Проверку в сущности отвергли тоже: для неё сущности пришлось бы
принимать родителя или список предков, а это решение принимается в use
case, а не при создании объекта. В этапе 1 use case переноса нет,
категории создаются только онбордингом через `new_child`, где цикл
невозможен. Правило понадобится тогда, когда появится управление
категориями. Три других инварианта уникальности (имя счёта, имя и `path`
категории, `external_key` операции) ревьюер замечаниями не оформлял: их
закрывают уникальные индексы схемы на подзадаче 8.

**Проверка.** Ревьюер тестов не запускал: так было задано, потому что код
не менялся. После правки документа главная сессия пересобрала выжимку
командой `make brief`. `make lint` зелёный, включая сверку выжимки с
документом. `make test` — 108 passed.

**Открытые вопросы.** По-прежнему в таблице `categories` раздела 4.3 нет
столбца `aliases`. Его нужно добавить до подзадачи 8.

**Следующий шаг.** Подзадача 4 «Порты и DTO» слоя `application` исполнителем
`developer`. Запускать в новой сессии.

## 2026-09-23 — Модульные тесты доменного ядра этапа 1

- **Ветка / коммит:** feat/stage1-bot-ledger / поверх 538213f
- **Этап:** 1, подзадача 2 плана; разделы 3.1–3.3, 4.2 и 11.1 `docs/architecture.md`

**Сделано.** Доменное ядро этапа 1 покрыто модульными тестами в
`tests/unit/domain/`: по одному файлу на `Currency`, `Money`, `Period`,
`Transaction`, `Category`, `Account` и `User`, всего 98 тестов. Для `Money`
проверены арифметика в одной валюте, запрет смешения валют, отказ принимать
`float` в конструкторе, умножении и делении, а также округление
`ROUND_HALF_UP` на граничных значениях. Результат округления сверяется с
прямым вызовом `Decimal.quantize`, который идёт в обход `Money`. Законы
сложения проверяются свойствами через `hypothesis`. Для `Transaction`
проверены знак суммы, связь вида операции с категорией и встречным счётом,
граница будущей даты и форма сторно: исходная операция переходит в
`reversed`, сторнирующая запись сразу создаётся в `reversed`, а повторное
сторно отклоняется. Для дефолтного набора категорий проверены количество
33/27/6, уникальность `path` и то, что родитель каждой категории есть в
наборе и имеет тот же вид. Тесты написал исполнитель `tester` по файлу
задачи `docs/tasks/done/2026-09-23-domain-unit-tests.md`. Пустой
`tests/unit/domain/.gitkeep` удалён, потому что каталог больше не пуст.

**Решения.** Исполнителю запретили чинить код, если тест расходится с
документом. Такой тест он должен был пометить
`xfail(strict=True)` и вернуть вопрос. Иначе тест подгоняется под
реализацию, и расхождение с документом исчезает без следа. Расхождений не
нашлось. У `User` в коде нет своей валидации, поэтому отрицательный случай
проверяет неизменяемость `telegram_id` через frozen-датакласс. Правило,
которого нет в разделе 3.3, для теста не придумывалось. Будущая дата
операции проверяется с явно переданным моментом времени, системные часы
тесты не читают.

**Проверка.** `make test` — 108 passed, интеграционные тесты тоже
прогнались. `make lint` зелёный: выжимка совпадает с документом, `ruff
check`, `ruff format --check` и `mypy src` без ошибок. Проверки повторила
главная сессия после отчёта исполнителя.

**Открытые вопросы.** Прежний остаётся: в таблице `categories` раздела 4.3
нет столбца `aliases`, а на алиасах держится разрешение категории из
раздела 6.5. Пробел нужно закрыть до подзадачи 8.

**Следующий шаг.** Подзадача 3 плана: исполнитель `reviewer` проверяет
`git diff` подзадач 1–2.

---

## Архив

Записи, вытесненные из этого файла по правилу объёма, лежат в
[`docs/journal/2026-Q3.md`](journal/2026-Q3.md).
